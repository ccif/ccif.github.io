\documentclass[11pt]{article}

% Essential packages
\usepackage[letterpaper,margin=0.5in]{geometry}  % set page size and margins
\usepackage[hidelinks]{hyperref}                 % format hyperlinks
\usepackage{enumitem}                            % enable lists for bullet points: itemize and \item
\usepackage{titlesec}                            % enable section title customization
\usepackage{XCharter}                            % set font to XCharter
\usepackage[T1]{fontenc}                         % output encoding
\usepackage[utf8]{inputenc}                      % input encoding
\usepackage{ragged2e}                            % enable ragged right text alignment
\pagestyle{empty}                                % remove page numbers

% Set ragged right alignment for the entire document
\RaggedRight

% ensure PDF output will be all-Unicode and machine-readable
\input{glyphtounicode}
\pdfgentounicode=1

% Custom commands
\newcommand{\entrytitle}[1]{\vspace{0.5em}\textbf{#1}}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=blue,
}

% Title formatting
\titleformat{\section}{\Large\bfseries}{}{0em}{}[\titlerule]
\titlespacing*{\section}{0em}{0.5em}{0.2em}

% List settings
\setlist[itemize]{leftmargin=*,nosep}
\setlist[enumerate]{leftmargin=*,nosep}

% Document structure
\begin{document}

% Name
\centerline{\Huge{$name$}}

% Contact information
\centerline{$for(contact)$
  $contact.email$ \textbar{} $contact.portfolio$ \textbar{} $contact.github$
$endfor$}

% Experience
\section{Experience}
$for(experience)$
\entrytitle{$experience.title$}, $experience.company$ \hfill $experience.dates$ \\
\begin{itemize}
$for(experience.bullets)$
    \item $experience.bullets$
$endfor$
\end{itemize}
$endfor$

% Projects
\section{Projects}
$for(projects)$
\entrytitle{$projects.title$}$if(projects.url)$ \hfill $projects.url$$endif$ \\
$if(projects.technologies)$\textit{$projects.technologies$}$endif$
\begin{itemize}
$for(projects.bullets)$
    \item $projects.bullets$
$endfor$
\end{itemize}
$endfor$

% Skills
\section{Skills}
\begin{itemize}[label={},leftmargin=0em]
$for(skills)$
    \item \textbf{$skills.category$:} $skills.items$
$endfor$
\end{itemize}

% Education
\section{Education}
$for(education)$
\textbf{$education.school$} \\
$education.degree$ \hfill $education.honors$, $education.date$
$endfor$

\end{document}